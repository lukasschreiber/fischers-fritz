{
  "name": "@fischers-fritz/types",
  "workspaces": [
    "frontend",
    "backend",
    "types"
  ],
  "scripts": {
    "dev":  "npm run dev -w types && concurrently --kill-others \"npm run dev -w frontend\" \"npm run dev -w backend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.1"
  }
}
