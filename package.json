{
  "name": "fischers-fritz",
  "workspaces": [
    "frontend",
    "backend",
    "types"
  ],
  "scripts": {
    "dev:backend": "cd backend && .\\venv\\Scripts\\activate && uvicorn main:app --reload --port 3000",
    "dev": "npm run dev -w frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.1"
  },
  "dependencies": {
    "react-router-dom": "^6.16.0"
  }
}
