server {
    listen 80;
    
    # Serve static files
    location / {
        root /usr/share/nginx/html;
        try_files $uri $uri/ /index.html;
    }

    # Error handling
    error_page 404 /index.html;

    location = /index.html {
        root /usr/share/nginx/html;
    }
}